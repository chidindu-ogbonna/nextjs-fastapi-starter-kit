[tool.pyright]
venvPath = "."
venv = ".venv"
useLibraryCodeForTypes = false

[tool.pytest.ini_options]
pythonpath = "."
asyncio_mode = "auto"

[tool.poetry.dependencies]
fastapi = "^0.110.0"
uvicorn = "^0.27.1"
